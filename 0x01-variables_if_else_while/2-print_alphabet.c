#include <stdio.h>

/**
 * main - print alphabet in lowercase
 *
 * Return: 0 Success
 */
int main(void)
{
	char ch = 'a'
		putchar("Alphabets in lowercase:\n");
	while (c <= 'z')
	{
		putchar("%c", ch);
		ch++;
	}
	return (0);
}
