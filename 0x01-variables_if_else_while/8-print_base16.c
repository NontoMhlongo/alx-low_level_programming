#include <stdio.h>
/**
 * main - Start the code
 *
 * Return: 0 Success
 */
int main(void)
{
	int d;
	int c;
	
	for (d = 48; d <= 57; d++)
	{
		putchar(d);
	}
	for (c = 97; c <= 102; c++)
	{
		putchar(c);
	}
putchar('\n');	
	return (0);
}
